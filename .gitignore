# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Generated Prisma client
generated

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# User-uploaded content (kept outside public/ and .output/ so it survives
# rebuilds — see server/routes/uploads/products/[filename].get.ts)
storage/uploads/

# Real o2switch connection details (copy deploy.config.example). The
# *.config wildcard also covers named per-target files like
# deploy.logicielbi.config — see deploy/deploy.sh's target-name argument.
deploy/deploy.config
deploy/deploy.*.config
